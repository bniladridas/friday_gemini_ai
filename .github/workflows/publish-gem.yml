# SPDX-License-Identifier: MIT
# Copyright (c) 2026 friday_gemini_ai

---
name: Publish Gem to GitHub Packages

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Version to publish (e.g., 1.5.0)'
        required: true
        type: string
      skip_tests:
        description: 'Skip tests (use with caution)'
        required: false
        type: boolean
        default: false

permissions:
  contents: write
  packages: write

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest
    if: ${{ !inputs.skip_tests }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Set up Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.3'
          bundler-cache: true

      - name: Run tests
        run: bundle exec ruby tests/runner.rb
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}

  build:
    name: Build Gem
    runs-on: ubuntu-latest
    needs: [test]
    if: always() && (needs.test.result != 'failure' || inputs.skip_tests)

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Set up Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.3'
          bundler-cache: false

      - name: Configure Bundler
        run: bundle config set frozen false

      - name: Install dependencies
        run: bundle install

      - name: Build gem
        run: gem build *.gemspec

      - name: Upload gem artifact
        uses: actions/upload-artifact@v7
        with:
          name: gem-package
          path: "*.gem"

  publish-github-packages:
    name: Publish to GitHub Packages
    runs-on: ubuntu-latest
    needs: build
    permissions:
      contents: read
      packages: write

    steps:
      - name: Download gem artifact
        uses: actions/download-artifact@v8
        with:
          name: gem-package

      - name: Publish to GitHub Packages
        run: |
          gem push *.gem --host https://rubygems.pkg.github.com/${{ github.repository_owner }} --key github
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  publish-rubygems:
    name: Publish to RubyGems
    runs-on: ubuntu-latest
    needs: build
    if: false  # Disabled - use manual workflow for RubyGems publishing

    steps:
      - name: Download gem artifact
        uses: actions/download-artifact@v8
        with:
          name: gem-package

      - name: Publish to RubyGems
        run: |
          gem push *.gem --host https://rubygems.org --key rubygems_api_key
        env:
          RUBYGEMS_API_KEY: ${{ secrets.RUBYGEMS_API_KEY }}
